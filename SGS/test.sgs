let integer rounds be 1000000.
let integer sum be 0.

print an int with value current time.
loop when rounds > 0
	let sum be sum+1.
	let rounds be rounds-1.
end loop.
print an int with value current time.
print an int with value sum.