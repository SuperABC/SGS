let integer iter be 100.
let array arr be 0 1 2.

new function test.
start test. 
	let result be 0. 
	loop when iter > 0. 
		let result be result + iter. 
		let iter be iter - 1. 
	end loop. 
end test.

outln with content test.
outln with content 'hello' + ' ' + 'world'.
outln with content arr.

let arr[0] be 3.
let arr[1] be 4.
let arr[2] be 5.
outln with content arr.